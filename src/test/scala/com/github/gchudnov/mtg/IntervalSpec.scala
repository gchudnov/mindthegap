package com.github.gchudnov.mtg

import com.github.gchudnov.mtg.types.IntInterval

final class IntervalSpec extends TestSpec:
  "Interval" when {
    "IntInterval" should {
      "comparable" in {
        // val x = IntInterval(1, 2)
        // val y = IntInterval(1, 2)
      }
    }
  }
